FROM python:3.10-slim

WORKDIR /app

COPY vuln_lab/ /app/vuln_lab/

RUN pip install --no-cache-dir flask==3.0.3 gunicorn==22.0.0 werkzeug==3.0.3

ENV PORT=8080
ENV PYTHONUNBUFFERED=1

CMD exec gunicorn --bind :$PORT --workers 2 --threads 4 --timeout 0 "vuln_lab.app:app"
